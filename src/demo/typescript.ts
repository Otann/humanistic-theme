class MyClass {
  public static myValue: string;
  constructor(init: string) {
    this.myValue = init;
  }
}
import fs = require("fs");
module MyModule {
  export interface MyInterface extends Otherr {
    myProperty: any;
  }
}
const foo = 100000000
const m = new MyClass('a');

declare magicNumber number;
myArray.forEach(() => { }); // fat arrow syntax
